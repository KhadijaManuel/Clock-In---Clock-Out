RewriteEngine On
RewriteBase /Clock-In---Clock-Out/public/

# Route all /api/... calls to the API index
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^api/(.*)$ api/index.php [L,QSA]

Options -Indexes
